spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url : jdbc:mariadb://localhost:3306/catchtable3
    username : root
    password : 15243521
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  jpa:
    defer-datasource-initialization : true
    show-sql: true
    hibernate:
      ddl-auto : create-drop

  profiles:
    include : jwt

